package juegos;

/** Resultado de una ronda de "Piedra, papel o tijera". */
public enum ResultadoRonda {
    /** El usuario ganó la ronda. */
    GANA_USUARIO,
    /** La máquina ganó la ronda. */
    GANA_MAQUINA,
    /** Ambos eligieron la misma opción. */
    EMPATE
}
